{
  "name": "berolab-bot",
  "version": "1.0.0",
  "description": "Bot para acessar berolab.app diariamente",
  "main": "script-render.ts",
  "scripts": {
    "start": "ts-node script-render.ts",
    "build": "tsc script-render.ts",
    "test": "npm run start"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}